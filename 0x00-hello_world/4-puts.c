#include <stdio.h>

/**
 * main - function that print a string to the <stdo> using 'Puts'
 *
 * Return: (0) Always
 */

int main(void)
{
puts("""\"Programming is like building a multilingual puzzle\n");
return (0);
}
